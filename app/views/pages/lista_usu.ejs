<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <% if (dadosPagina) { %>
        <% dadosPagina[0].forEach(usuario => { %>
            <a href="/perfil-eleitor/<%= usuario.idUsuario %>">
                <%= usuario.nomeUsuario %>
            </a>
            <br>
        <%})%>
    <%}%>
</body>
</html>